export const initKeyboardShortcuts = () => {
    // Keyboard shortcuts are now handled globally in app.js to prevent CORS issues
    // and ensure compatibility with both legacy and modern code.
    console.log('FSD Keyboard shortcuts diverted to global app.js listener.');
};
